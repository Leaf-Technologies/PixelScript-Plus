name: Build and Draft Release for PixelScript-Plus

on:
  push:
    paths:
      - 'PixelScript.py'
      - '.github/workflows/build-and-release.yml'
    tags:
      - '*'
  workflow_dispatch:

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      file_version: ${{ steps.get_file_version.outputs.file_version }}
      release_version: ${{ steps.get_release_version.outputs.release_version }}
      version_is_newer: ${{ steps.compare_versions.outputs.version_is_newer }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get CURRENT_VERSION from PixelScript.py
        id: get_file_version
        run: |
          FILE_VERSION=$(sed -nE "s/^.*CURRENT_VERSION\s*=\s*[\"']([^\"']+)[\"'].*$/\1/p" PixelScript.py)
          if [[ -z "$FILE_VERSION" ]]; then
            echo "CURRENT_VERSION not found in PixelScript.py!"
            exit 1
          fi
          echo "file_version=$FILE_VERSION" >> $GITHUB_OUTPUT

      - name: Get latest release version from GitHub API
        id: get_release_version
        run: |
          DATA=$(curl -sSf https://api.github.com/repos/Northy2410/PixelScript-Plus/releases/latest || true)
          TAG=$(echo "$DATA" | jq -r '.tag_name')
          if [[ "$TAG" == "null" || -z "$TAG" ]]; then
            RELEASE_VERSION="0.0.0"
          else
            RELEASE_VERSION=$(echo "$TAG" | sed 's/^[vV]//')
          fi
          echo "release_version=$RELEASE_VERSION" >> $GITHUB_OUTPUT

      - name: Compare file version with latest release
        id: compare_versions
        run: |
          echo "File version: '${{ steps.get_file_version.outputs.file_version }}'"
          echo "Release version: '${{ steps.get_release_version.outputs.release_version }}'"
          python3 <<'EOF'
import sys
def parse(ver):
    return tuple(int(x) for x in ver.strip().split('.'))
file_version = "${{ steps.get_file_version.outputs.file_version }}"
release_version = "${{ steps.get_release_version.outputs.release_version }}"
try:
    fv = parse(file_version)
    rv = parse(release_version)
    print(f"Comparing: {fv} > {rv}")
    if fv > rv:
        print("version_is_newer=true")
    else:
        print("version_is_newer=false")
except Exception as e:
    print("Error parsing versions:", e)
    print("version_is_newer=false")
EOF | tee result.txt
          grep "version_is_newer=" result.txt | tee /dev/stderr | sed 's/^/::set-output name=version_is_newer::/' | while read line; do
            VAL=$(echo "$line" | sed 's/version_is_newer=//')
            echo "version_is_newer=$VAL" >> $GITHUB_OUTPUT
          done

      - name: Show version compare result
        run: |
          echo "Version in file: ${{ steps.get_file_version.outputs.file_version }}"
          echo "Latest release version: ${{ steps.get_release_version.outputs.release_version }}"
          echo "File version is newer: ${{ steps.compare_versions.outputs.version_is_newer }}"

  build-test-package:
    needs: check-version
    if: needs.check-version.outputs.version_is_newer == 'true'
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    env:
      RELEASE_TAG: ${{ needs.check-version.outputs.file_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install pyinstaller requests

      # --- Test Step ---
      - name: Test PixelScript.py Launch
        run: |
          python PixelScript.py &
          sleep 10
          pkill -f PixelScript.py || true

      # --- Build Portable (No-Installer) ---
      - name: Build Portable Executable/App
        run: |
          pyinstaller PixelScript.py --onefile --windowed --name "PixelScript+-No-Installer"

      # --- Build Installer ---
      - name: Build Installer
        if: runner.os == 'Windows'
        run: |
          pyinstaller PixelScript.py --onefile --windowed --name "PixelScript+-Installer"
          if exist installer.nsi (
            makensis /DOUTPUTFILE="dist\PixelScript+-Installer.exe" installer.nsi
            move /Y "dist\PixelScript+-Installer.exe" "dist\PixelScript+-Installer.exe"
          )

      - name: Build Installer (macOS)
        if: runner.os == 'macOS'
        run: |
          pyinstaller PixelScript.py --onefile --windowed --name "PixelScript+-Installer"
          hdiutil create -volname "PixelScriptPlus" -srcfolder dist/ -ov -format UDZO dist/PixelScript+-Installer.dmg

      - name: Build Installer (Linux)
        if: runner.os == 'Linux'
        run: |
          pyinstaller PixelScript.py --onefile --windowed --name "PixelScript+-Installer"
          # Example for AppImage or .deb packaging could go here

      - name: Rename installer output (macOS)
        if: runner.os == 'macOS'
        run: mv dist/PixelScript+-Installer.dmg dist/PixelScript+-Installer

      - name: List artifacts
        run: ls dist/

      - name: Upload release assets
        uses: actions/upload-artifact@v4
        with:
          name: release-assets-${{ matrix.os }}
          path: |
            dist/PixelScript+-No-Installer*
            dist/PixelScript+-Installer*

  create-draft-release:
    needs: build-test-package
    if: always()
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.check-version.outputs.file_version }}
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Draft Release and Upload Assets
        if: needs.build-test-package.result == 'success'
        uses: softprops/action-gh-release@v1
        with:
          draft: true
          tag_name: ${{ env.RELEASE_TAG }}
          name: "PixelScript-Plus ${{ env.RELEASE_TAG }}"
          files: 'artifacts/**/*'

      - name: Display Release Summary
        if: needs.build-test-package.result == 'success'
        run: |
          echo "Draft release created for PixelScript-Plus version ${{ env.RELEASE_TAG }}"
      - name: Output when skipped
        if: needs.build-test-package.result != 'success'
        run: echo "No new build was created since version is not newer than release."
